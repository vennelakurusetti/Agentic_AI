# Prompt Doctor — Specification

Purpose
- Interactive prompt engineering tutor with automated examiner grading.

Components
- `app.py`, `examiner.py`, `levels.py`, `runner.py`.

Run
- `pip install streamlit requests python-dotenv` and run `streamlit run app.py`.
