# Prompt Pipeline Builder — Specification

Purpose
- Browser-based tool to design and test multi-stage LLM prompt chains.

Components
- `index.html` — UI and pipeline runner.

Run
- Open `index.html` in a browser; to use a real LLM, replace `callLLM()` and add your API key.
